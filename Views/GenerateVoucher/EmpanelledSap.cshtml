
@{
    ViewBag.Title = "EmpanelledSap";
    Layout = "~/Views/Shared/_TuvVision.cshtml";
}

<link href="~/AllJsAndCss/css/filepond.css" rel="stylesheet" />
<script src="~/AllJsAndCss/js/filepond.js"></script>

<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css" />


<link rel="stylesheet" href="https://cdn.datatables.net/1.11.3/css/jquery.dataTables.min.css">
<script src="https://cdn.datatables.net/1.11.3/js/jquery.dataTables.min.js"></script>

<script>
    FilePond.parse(document.body);



</script>

<style>
    .height-table {
        height: auto;
    }

    body {
        font-family: 'TNG Pro';
    }

    table {
        width: 100%;
        border-collapse: collapse;
        margin-bottom: 20px;
    }

    th, td {
        border: 1px solid #ddd;
        padding: 8px;
        text-align: center;
        font-family: 'TNG Pro';
    }

    th {
        background-color: #001ED2;
        color: #ffffff;
        font-family: 'TNG Pro';
        font-weight: normal;
    }

    @@media (max-width: 600px) {
        table {
            border: 0;
        }

            table thead {
                display: none;
            }

            table tr {
                margin-bottom: 10px;
                display: block;
                border-bottom: 2px solid #ddd;
            }

            table td {
                display: block;
                text-align: right;
            }

                table td:last-child {
                    border-bottom: 0;
                }
    }

    .label1 {
        font-weight: normal !important;
    }

    #preloader {
        position: fixed;
        width: 100%;
        height: 100vh;
        background: #fff url('../../Content/Loader/loader.gif') no-repeat center center;
        z-index: 9999;
        /*background: url('../../Content/Loader/loader.gif')no-repeat center center;*/
    }

    .highlight-row {
        background-color: #FFEB00;
    }

    label {
        font-weight: normal;
        font-family: 'TNG Pro';
    }

    .highlight-row_ {
        background-color: #EFEFEF;
    }

    .inline-elements h4,
    .inline-elements input {
        display: inline-block;
        margin-right: 20px; /* Adjust spacing between elements as needed */
</style>



<div class="main-content3 xs-max-height">

    <!--Start of header-->
    <div class="container-fluid">
        <div>
            <form method="post">
                <div class="col-xs-12">
                    <h3 class="dynamic-page-title dynamic-page-title-2  text-left" style="font-family:'TNG Pro'">Travel Expenses Voucher(s) List</h3>
                    <button type="button" value="Approved" class="btn btn-info dynamic-function-btn dynamic-function-btn-2" id="btnSapDownload" onclick="GetSap_UploadData()" style="border:none;margin-right:68px;margin-top: 14px;font-family:'TNG Pro';">
                        Sap Download
                    </button>
                    <button type="button" value="Approved" class="btn btn-info dynamic-function-btn dynamic-function-btn-2" onclick="location.href='@Url.Action("GenerateVoucherApproval", "GenerateVoucher")'"
                            style="border:none;margin-top: 13px; margin-right:35px;font-family:'TNG Pro';">
                        Go Back
                    </button>
                </div><!-- /.col -->
            </form>

            <div class="container-fluid">
                <div class="inline-elements">
                    <h4>Count</h4>
                    <input type="text" id="txtcount" />

                    <h4>Total Amount</h4>
                    <input type="text" id="totalamount_" />

                    <h4>Approved Amount</h4>
                    <input type="text" id="approvedamount_" />
                </div>
            </div>

            <input type="hidden" id="hdnpkexpenseid" name="hdnpkexpenseid" />


            @{
                int count = 0;
            }

            <div class="col-xs-12">

                <table id="example" class="display custom-table no-footer" cellspacing="0">
                    <thead>
                        <tr>
                            <th class="remove-search" style="width:2%;">
                                <input type="checkbox" id="chkChkAll" />
                            </th>
                            <th style="font-family:'TNG Pro';" class="remove-search"><label>Voucher No</label></th>
                            <th style="font-family:'TNG Pro';" class="remove-search"><label>Month</label></th>
                            <th style="font-family:'TNG Pro';" class="remove-search"><label>Inspector Name</label></th>
                            <th style="font-family:'TNG Pro';" class="remove-search"><label>Employee Code</label></th>
                            <th style="font-family:'TNG Pro';" class="remove-search"><label>Employement Category</label></th>
                            <th style="font-family:'TNG Pro';" class="remove-search"><label>Days Present</label></th>
                            <th style="font-family:'TNG Pro';" class="remove-search"><label>Total Amount</label></th>
                            <th style="font-family:'TNG Pro';" class="remove-search"><label>Approved Amount</label></th>
                            <th style="font-family:'TNG Pro';" class="remove-search"><label>Remark</label></th>
                            <th style="font-family:'TNG Pro';" class="remove-search"><label>Previous Remark</label></th>

                        </tr>
                    </thead>
                </table>

            </div><!-- /.box-shadow -->
            <div class="col-xs-12">
                <input type="button" id="btnApprove" value="Approve" class="btn btn-default insert-form-btn" style="margin-bottom: 40px;" />
            </div>
        </div><!-- /.container-fluid -->
    </div><!-- /.row -->
</div>

@*New Added Code End*@
<div class="modal fade" id="remarksModal" tabindex="-1" role="dialog" aria-labelledby="remarksModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="remarksModalLabel"></h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <!-- Table to display remarks -->
                <div class="table">
                    <table class="table table-condensed table-responsive " id="tblusersmodal" style="border:1px;font-size:12px;">
                        <thead style="background-color:#001ED2;color:white;border:1px solid;">
                            <tr class="" style="background-color:#001ED2;font-family:'TNG Pro';color:white;">
                                <th scope="col" style="font-size: 14px;font-family:'TNG Pro';font-weight:normal;">Date</th>
                                <th scope="col" style="font-size: 14px;font-family:'TNG Pro';font-weight:normal;">Approval Stage</th>
                                <th scope="col" style="font-size: 14px;font-family:'TNG Pro';font-weight:normal;">Approver Name</th>
                                <th scope="col" style="font-size: 14px;font-family:'TNG Pro';font-weight:normal;">Remarks</th>
                                <th scope="col" style="font-size: 14px;font-family:'TNG Pro';font-weight:normal;">Approved Amount (INR)</th>
                                @*<th scope="col" style="font-size: 14px;font-family:'TNG Pro';">Total Amount approved</th>*@
                            </tr>
                        </thead>
                        <tbody id="userbody" style="border:1px solid;font-size:13px;font-family:'TNG Pro';color: black;"></tbody>
                    </table>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
                </div>
            </div>
        </div>
    </div>
</div>



@{
    var UrlLink = System.Configuration.ConfigurationManager.AppSettings["UrlLink"];
}


<script type="text/javascript">
    $(document).ready(function () {
        debugger;
        //$("body").prepend('<div id="preloader"></div>');
        GetVoucherList();
        var UrlLink = '@UrlLink';
        var rows = $('#example tbody tr').length;
        for (var i = 1; i <= rows; i++) {
            var totalAmount = $("#tottalAmounts_" + i).val();
            let approvedAmount = $("#amount_" + i).val();
            if (totalAmount != approvedAmount) {
                $("#txttr_" + i).addClass('highlight-row');
            }
        }
        $('#chkChkAll').click(function () {
            if ($(this).is(":checked")) {
                $(".chkCheckBoxId").prop("checked", true)
            }
            else {
                $(".chkCheckBoxId").prop("checked", false)
            }
            calculateCheckedValues();
        });
        $(document).on('change', 'input[type="checkbox"]', function () {
            calculateCheckedValues();
        });
    });


    $('#remarksInfoIcon').on('click', function () {
        $('#remarksModal').modal('show'); // Use your modal ID here
    });



    //added by nikita on 22042024
    function calculateCheckedValues() {
        var checkedCheckboxes = $('input[type="checkbox"]:checked').not('#chkChkAll'); // Exclude the "Check All" checkbox
        var totalCount = checkedCheckboxes.length; // Count of checked checkboxes
        var totalAmount = 0;
        var approvedAmount = 0;

        checkedCheckboxes.each(function () {
            var row = $(this).closest('tr'); // Get the closest row
            totalAmount += parseFloat(row.find('td:eq(7)').text());
            approvedAmount += parseFloat(row.find('td:eq(8)').text());
        });

        $('#txtcount').val(totalCount);
        $('#totalamount_').val(totalAmount.toFixed(2));
        $('#approvedamount_').val(approvedAmount.toFixed(2));
    }
    //end

    function GetVoucherList() {
        debugger;
        $("body").prepend('<div id="preloader"></div>');
        var UrlLink = '@UrlLink';
        $.ajax({
            type: "GET",
            url: UrlLink + "/GenerateVoucher/GetVoucherforEmpannelsap",
            contentType: "application/json; charset=utf-8",
            dataType: "json",
            success: function (response) {
                if (!!response) {
                    var a = JSON.parse(response);
                    var aa = JSON.stringify(response);

                    if (a == "Error") {
                        $("#example tbody").empty();
                        tr = $('<tr />');
                        tr.append('<td colspan=11 style="text-align: center;">No Record Found</td>');
                        $('#example').append(tr);
                    }
                    else {

                        $("#example tbody").empty();
                        for (var i = 0; i < a.length; i++) {
                            tr = $('<tr />');
                            tr.append("<td><input type='checkbox' id='" + a[i].VoucherNo + "' /> </td>");
                            tr.append("<td><a href=" + UrlLink + "'/GenerateVoucher/VoucherHistorApprovedList?VoucherNo=" + a[i].VoucherNo + "'>" + a[i].VoucherNo + "</a></td>");
                            //added by nikita on 22112023
                            tr.append("<td><label'>" + a[i].Month_Name + "</label> </td>");

                            tr.append("<td><label'>" + a[i].Name + "</label> </td>");
                            tr.append("<td><label'>" + a[i].EmployeeCode + "</label> </td>");
                            tr.append("<td><label'>" + a[i].EmployementCategory + "</label> </td>");
                            //end
                            tr.append("<td><label'>" + a[i].DaysPresent + "</label> </td>");
                            tr.append("<td><label'>" + a[i].TotalAmount + "</label> </td>");
                            tr.append("<td><label'>" + a[i].ApprovedAmount + "</label> </td>");
                            //if (a[i].TotalAmount != a[i].ApprovedAmount) {
                            //    .highlight-row_
                            //}
                            if (a[i].TotalAmount != a[i].ApprovedAmount) {
                                // Add a class to highlight the row
                                tr.addClass("highlight-row_");
                            }
                            tr.append("<td style='width:20%;'><label'>" + a[i].Status + "</label></td>");
                            tr.append("<td><i class='fas fa-info-circle' id='icon_" + i + "' style='cursor: pointer;' onclick='BindDescription(\"" + a[i].VoucherNo + "\")'></i></td>");

                            $('#example').append(tr);

                        }
                    //    $('#example').DataTable({
                    //        "paging": true,
                    //        "searching": true,
                    //        "ordering": true,
                    //        "info": true,
                    //        "lengthMenu": [10, 25, 50, 75, 100],
                    //        "fnRowCallback": function (nRow, aData, iDisplayIndex, iDisplayIndexFull) {
                    //            debugger;
                    //            if (aData[7] !== aData[8]) {
                    //                $('td', nRow).css('background-color', '#FED7DA')
                    //            }
                    //            else {
                    //            }
                    //        }
                    //    });

                        //}
                        $('#example').DataTable({
                            "paging": true,
                            "searching": true,
                            "ordering": true,
                            "info": true,
                            "lengthMenu": [10, 25, 50, 75, 100],
                            "columnDefs": [{
                                "targets": [0], // Target the first column (checkboxes)
                                "orderable": false,
                                "className": "select-checkbox",
                                "checkboxes": {
                                    "selectRow": true
                                }
                            }],
                            "select": {
                                "style": "multi"
                            },
                            "initComplete": function () {
                                // Remove unwanted classes from table headers
                                $('#example thead th').removeClass('remove-search');
                            }
                        });
                        $('#chkChkAll').on('click', function () {
                            if ($(this).is(":checked")) {
                                table.rows().select();
                            } else {
                                table.rows().deselect();
                            }
                            calculateCheckedValues(); // Recalculate values when "Check All" checkbox is clicked
                        });

                        // Recalculate values when rows are selected/deselected
                        table.on('select deselect', function () {
                            calculateCheckedValues();
                        });
                    }
                    $("#preloader").remove();

                }
                else {
                    // Handle the case when response is null or empty
                    $("#example tbody").empty();
                    tr = $('<tr />');
                    tr.append('<td colspan=11 style="text-align: center;">No Record Found</td>');
                    $('#example').append(tr);
                    $("#preloader").remove();

                }
            },

            error: function (xhr, status, error) {
                debugger;
                $("#preloader").remove();
                // Code to handle errors goes here
                //hideLoader(); // Hide loader when the content is loaded
            }
        });
        $("#preloader").remove();

    };
    $('#chkChkAll').click(function (e) {
        var table = $(e.target).closest('table');
        $('td input:checkbox', table).prop('checked', this.checked);
    });


    function BindDescription(d) {
        debugger;
        var VoucherNo = d;
        var UrlLink = '@UrlLink';

        $.ajax({
            ////url: UrlLink + "/GenerateVoucher/Voucher_History_Description",
        url: "@Url.Action("Voucher_History_Description", "GenerateVoucher")",
        type: "get",
        DataType: "json",
        async: false,
        contenttype: "application/json; charset=utf-8",
        data:{VoucherNo:VoucherNo},
        success: function (result) {
            debugger;
            $("#tblusersmodal tbody").empty();

            var a = JSON.parse(result);

            let date = [a[0].Date, a[0].PCH_Approval_Date, a[0].CH_Approval_Date, a[0].Account_Approval_Date, a[0].FI_Approval_Date]

            let Name_ = [a[0].Name, a[0].PCH_Name, a[0].CH_Approval, a[0].Account_Approval, a[0].FI_Approval]

            let Amount = [a[0].Approval_One_Approval_Amount, a[0].Pch_ApprovalAmount, a[0].Ch_Approval_Amount, a[0].Accountant_Approval_Amount, a[0].FI_Approval_Amount]

            let Data = ["ApprovalOne/ApprovalTwo","PCH","Cluster Head", "Accountant","Finance"]
            let k = 0;
            var myArray = [];
            for(var i=0;i<a.length;i++)
            {
                if(!!a[i].ApprovalOne_Description)
                {
                    myArray.push(a[i].ApprovalOne_Description);
                    k++;
                }
                if(!!a[i].PCH_Approval_Description)
                {
                    myArray.push(a[i].PCH_Approval_Description);
                    k++;

                }
                if(!!a[i].CH_Approval_Description)
                {
                    myArray.push(a[i].CH_Approval_Description);
                    k++;

                }
                if (!!a[i].Accountant_Description)
                {
                    myArray.push(a[i].Accountant_Description);
                    k++;
                }
                if (!!a[i].Finance_Description) {
                    myArray.push(a[i].Finance_Description);
                    k++;
                }

                for (var j = 0; j < myArray.length; j++)
                {
                    //const approvalName = Name[j] == undefined ? "-" : Name[j];
                    // changes added by nikita on 22112023
                    $("#tblusersmodal").append('<tr><td>' + date[j] + '</td><td>' + Data[j] + '</td><td>' + Name_[j] + '</td><td>' + myArray[j] + '</td><td>' + Amount[j] + '</td></tr>')
                }

            }
            $('#remarksModal').modal('show');

        },
        error: function (result) {
            debugger;
            (result+"something went wrong");
        }
    });
    }

    function GetSap_UploadData() {

        // Function to trigger file download
        debugger;
        $("body").prepend('<div id="preloader"></div>');
        var i = 1;
        var message = "";
        var List_VoucherID = "";
        var Data = [];
        $("#example input[type=checkbox]:checked").each(function () {
            if ($(this).attr('id') === 'chkChkAll') {
                return true; // Skip to the next iteration
            }
            i++;
            var row = $(this).closest("tr")[0];
            var _rowIndex = row.rowIndex - 1;
            var item = {};
            var checkboxId = $(this).attr('id');
            var VoucherNo = '#example tbody tr:eq(' + _rowIndex + ') td:eq(1) input[type="text"]';
            item["VoucherNo"] = checkboxId;
            Data.push(item);
        });

        var Data1 = JSON.stringify(Data);
        //Create a form element
        var form = $('<form></form>');
        form.attr('action', '@Url.Action("SapDownloadForEmpanell", "GenerateVoucher")');
        form.attr('method', 'POST');
        form.attr('target', 'download-iframe'); // Target an iframe to handle the download response

        // Append hidden input fields with necessary data
        $('<input>').attr({
            type: 'hidden',
            name: 'Data1',
            value: Data1
        }).appendTo(form);


        // Append the form to the document body
        form.appendTo('body');

        // Create an iframe to handle the download response
        var iframe = $('<iframe name="download-iframe" style="display: none;"></iframe>');
        iframe.appendTo('body');

        // Submit the form
        form.submit();

        // Remove the form and iframe from the DOM after download
        setTimeout(function () {
            form.remove();
            iframe.remove();
            GetVoucherList();
        }, 100000); // Adjust timeout as needed

        $("#preloader").remove();
    }





    //added by nikita on 2204024

    $('#btnApprove').click(function (e) {
        debugger;
        if (confirm('Do you want to proceed with voucher(s)?')) {
            $("body").prepend('<div id="preloader"></div>');  //added by nikita on 05-02-2024
            ////var UrlLink = '@UrlLink';
            var i = 1;
            var message = "";
            var List_VoucherID = "";
            var Data = [];
            $("#example input[type=checkbox]:checked").each(function () {
                if ($(this).attr('id') === 'chkChkAll') {
                    return true; // Skip to the next iteration
                }
                i++;
                var row = $(this).closest("tr")[0];
                var _rowIndex = row.rowIndex - 1;
                var item = {};
                var checkboxId = $(this).attr('id');
                console.log('Checkbox ID: ' + checkboxId);
                var TotalAmountSelector_ = '#example tbody tr:eq(' + _rowIndex + ') td:eq(7) input[type="text"]';
                var TotalAmount = $(TotalAmountSelector_).val();
                var ApprovedAmountSelector = '#example tbody tr:eq(' + _rowIndex + ') td:eq(8) input[type="text"]';
                var ApprovedAmount = $(ApprovedAmountSelector).val();
                var ApprovedRemarksSelector = '#example tbody tr:eq(' + _rowIndex + ') td:eq(9) input[type="text"]';
                var ApprovedRemarks = $(ApprovedRemarksSelector).val();
                item["VoucherNo"] = checkboxId;
                item["Remarks"] = ApprovedRemarks;//$("#txtRemarks").val();
                item["ApprovedAmount"] = ApprovedAmount;
                item["TotalAmount"] = TotalAmount;
                Data.push(item);
            });
            var Data1 = JSON.stringify(Data);
            $.ajax({
                url: '@Url.Action("UpdateSapData", "GenerateVoucher")',
                type: "Post",
                dataType: "json",
                data: { Data1: Data1 },
                success: function (response) {
                    $('#preloader').remove();
                    $('#chkChkAll').prop('checked', false);
                    window.location.href = '@Url.Action("sapUpload", "GenerateVoucher")';
                },
                error: function (xhr, status, error) {
                    $('#preloader').remove();

                },
                complete: function () {
                    $("#preloader").remove();
                }
            });
        }
    });
</script>