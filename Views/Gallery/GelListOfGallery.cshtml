@model IEnumerable<TuvVision.BusinessEntities.Gallery_VM>

@{
    ViewBag.Title = "List Of Gallery";
    Layout = "~/Views/Shared/_TuvVision.cshtml";
}

@*@section header{*@
    <link href="~/Content/Css/extra.css" rel="stylesheet">

    <!--fancybox-->
    <link rel="stylesheet" href="~/Content/Css/fancybox/jquery.fancybox.css" media="screen">
    <link rel="stylesheet" href="~/Content/Css/fancybox/jquery.fancybox-thumbs.css" media="screen">
    <!--fancybox-->
    <!--datatable-->

<link href="~/Content/css/datatable/css/jquery.dataTables.css" rel="stylesheet" />
    <!--datatable-->

<script src="~/Content/css/datatable/js/jquery.dataTables.js"></script>
    <!--export to excel-->
    <script type="text/javascript" language="javascript" src="https://cdn.datatables.net/buttons/1.5.6/js/dataTables.buttons.min.js"></script>
    <script type="text/javascript" language="javascript" src="https://cdnjs.cloudflare.com/ajax/libs/jszip/3.1.3/jszip.min.js"></script>
    <script type="text/javascript" language="javascript" src="https://cdn.datatables.net/buttons/1.5.6/js/buttons.html5.min.js"></script>
    <script src="~/Scripts/Js/datatable-custom.js"></script>
    <!--datatable-->
   
    <!--fancybox-->
    <script src="~/Content/Css/fancybox/jquery.fancybox.js"></script>

<script src="~/AllJsAndCss/fancybox/jquery.fancybox.pack.js"></script>
    <script src="~/Content/Css/fancybox/jquery.fancybox-thumbs.js"></script>
    <script>
        $(function () {
            $('.fancybox-thumbs').fancybox({
                prevEffect: 'none',
                nextEffect: 'none',

                closeBtn: true,
                arrows: true,
                nextClick: true,

                helpers: {
                    thumbs: {
                        width: 88,
                        height: 50
                    }
                }
            });
        });
    </script>

    <!--equalheight-->

<script src="~/Scripts/Js/equalHeight.js"></script>
    <!-- End Other script files -->
@*}*@





<!--Start of main content 3-->
<div class="main-content2 xs-max-height">

    <!--Start of header-->
    <div class="container-fluid">
        <div class="row assignfixedwidth rp-15">

            <div class="col-xs-12">
                <h3 class="dynamic-page-title dynamic-page-title-2  text-left">Gallery List</h3>
                <a href="@Url.Action("AddEventGallery", "Gallery")" class="btn btn-info dynamic-function-btn dynamic-function-btn-2"><i class="fa fa-plus-circle"></i> Add Album </a>
            </div><!-- /.col -->
            <div>
                <input type="button" value="Delete Multiple" class="btn btn-info dynamic-function-btn" id="btnDelete" onclick="return confirm('Are you sure you want to Delete selected Record ?');" />
            </div>
            <div class="col-xs-12">
                <hr class="custom-hr">
            </div><!-- /.col -->
            <!--Table was here-->

        </div><!-- /.row -->

        <div class="col-xs-12">

            @{
                if (Convert.ToString(TempData["Success"]) != null && Convert.ToString(TempData["Success"]) != "")
                {
                    <h5 style="color:green;font-weight:bold;">@Convert.ToString(TempData["Success"])</h5>
                }
                if (Convert.ToString(TempData["Deleted"]) != null && Convert.ToString(TempData["Deleted"]) != "")
                {
                    <h5 style="color:red;font-weight:bold;">@Convert.ToString(TempData["Deleted"])</h5>
                }
                if (Convert.ToString(TempData["Error"]) != null && Convert.ToString(TempData["Error"]) != "")
                {
                    <h5 style="color:red;font-weight:bold;">@Convert.ToString(TempData["Error"])</h5>
                }
            }

            <span class="hidden-md hidden-lg" style="font-size:13px;">*Scroll below to view full table</span>
            <table id="example" class="display" cellspacing="0" width="100%">
                <thead>
                    <tr>
                        <th class="remove-search" style="text-align:center; width:50px !important;" valign="middle">
                            <input type="checkbox" id="checkBoxAll" style="opacity:inherit; text-align:center; display:block; left:12px; top: 32px; margin:auto; width:50px !important; display:block; position: relative;">
                        </th>

                        <th>Sr.No</th>
                        <th>Year</th>
                        <th>Event Title</th>
                        <th>Event Date</th>
                        <th>Image</th>
                        <th class="remove-search" style="text-align:center;">Action</th>
                    </tr>
                </thead>
                <tbody>
                    @if (ViewBag.Data != null)
                    {
                        int i = 0;
                        foreach (var item in ViewBag.Data as IEnumerable<TuvVision.BusinessEntities.Gallery_VM>)
                        {
                            i = i + 1;
                            <tr>
                                <td style="width:50px !important; margin:auto; text-align:center;">
                                    <input type="checkbox" name="ImgIds" class="checkBox" value="@item.PK_GalleryId" />
                                </td>
                                <th scope="row"> @i </th>
                                @*<td>@Convert.ToInt32(item.PK_NewsId).ToString()</td>*@
                                <td> @Convert.ToInt32(item.Year) </td>
                                <td> @item.Title</td>
                                <td> @Convert.ToDateTime(item.EventDate).ToString("dd MMM yyyy")</td>
                                <td>
                                    <img src="@Url.Content(item.Gallery)" height="50" alt="" />
                                </td>
                                <td class="action-btn" style="text-align:center;">
                                    @*<a href="#" class="btn btn-primary btn-xs"><i class="fa fa-folder"></i> View </a>*@
                                    <a href="~/Gallery/AddEventGallery?Id=@item.PK_GalleryId" class="edit" title="Edit"><i class="fa fa-pencil"></i></a>
                                    <a href="@Url.Action("DeleteGallery", "Gallery", new { id = item.PK_GalleryId })" onclick="return confirm('Are you sure you want to Remove this Record');" class="delete" title="Delete"><i class="fa fa-trash"></i></a>
                                </td>
                            </tr>
                        }
                    }


                </tbody>
            </table>

        </div><!-- /.data table col -->

    </div><!-- /.container-fluid -->
    <!--End of header-->

</div>
<!--End of main content 3-->


@*@section footer{*@
    <script>
        // Configure/customize these variables.
        var showChar = 30;  // How many characters are shown by default
        var ellipsestext = "...";
        var moretext = "See full";
        var lesstext = "See less";


        $('.more').each(function () {
            var content = $(this).html();

            if (content.length > showChar) {

                var c = content.substr(0, showChar);
                var h = content.substr(showChar, content.length - showChar);

                var html = c + '<span class="moreellipses">' + ellipsestext + '&nbsp;</span><span class="morecontent"><span>' + h + '</span>&nbsp;&nbsp;<a href="" class="morelink">' + moretext + '</a></span>';

                $(this).html(html);
            }

        });

        $(".morelink").click(function () {
            if ($(this).hasClass("less")) {
                $(this).removeClass("less");
                $(this).html(moretext);
            } else {
                $(this).addClass("less");
                $(this).html(lesstext);
            }
            $(this).parent().prev().toggle();
            $(this).prev().toggle();
            return false;
        });
    </script>

    <script>
        $(document).ready(function () {
            $("#example").DataTable();

            $('#checkBoxAll').click(function ()
            {
                $(".checkBox").prop('checked', $(this).prop('checked'));
            });
            $(".checkBox").click(function () {
                if ($(".checkBox").length == $(".checkBox:checked").length)
                {
                    $('#checkBoxAll').attr("checked", "checked");
                }
                else
                {
                    $('#checkBoxAll').removeAttr ("checked");
                }
            });
            

            $("#btnDelete").click(function () {
                var count = $(".checkBox:checked").length;
                var selectedIDs = new Array();

                if (count == 0)
                {
                    alert("Please select atleast one Image to Delete !")
                }
                else
                {
                    $('input:checkbox.checkBox').each(function () {
                        if ($(this).prop('checked')) {
                            selectedIDs.push($(this).val());
                        }
                    })
                }

                //var selectedIDs = new Array();
                //$('input:checkbox.checkBox').each(function () {
                //    if ($(this).prop('checked')) {
                //        selectedIDs.push($(this).val());
                //    }                
                //});

                var options = {};
                options.url = "@Url.Action("DeleteMultiple", "Gallery")";
                options.type = "POST";
                options.data = JSON.stringify(selectedIDs);
                options.contentType = "application/json";
                options.dataType = "json";
                options.success = function () { // previous code was function (msg)
                    //alert(msg);
                    location.href = "@Url.Action("GelListOfGallery", "Gallery")";
                };
                options.error = function () {
                    alert("Error while deleting the records!");
                };
                $.ajax(options);
            });
        });

    </script>
     <!--filterizr-->
<script src="~/Scripts/Js/jquery.filterizr.js"></script>
<script>
        $(function () {
            $('.filtr-container').filterizr();
            $('.gallery-filter-tab li').click(function () {
                $(this).addClass('filtr-active');
                $(this).siblings().removeClass('filtr-active');
            });
        });
</script>

@*}*@



<!------------------------------------------------------------------------------->
